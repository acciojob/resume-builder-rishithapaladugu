const { defineConfig } = require('cypress');

module.exports = defineConfig({
  // Configuration for the end-to-end testing
  e2e: {
    // Base URL for your application
    baseUrl: 'http://localhost:3000', // Change this to your app's URL

    // Spec pattern to match test files
    specPattern: 'cypress/e2e/**/*.{js,jsx,ts,tsx}',

    // Setup Node event listeners
    setupNodeEvents(on, config) {
      // Implement node event listeners here
      // Example: require('cypress-plugin-snapshots/plugin')(on);
    },

    // Additional options
    supportFile: 'cypress/support/index.js', // Path to support file
    fixturesFolder: 'cypress/fixtures', // Path to fixtures
    videosFolder: 'cypress/videos', // Path to save recorded videos
    screenshotsFolder: 'cypress/screenshots', // Path to save screenshots
    downloadsFolder: 'cypress/downloads', // Path for downloads
    video: true, // Record videos of test runs
    screenshotOnRunFailure: true, // Take screenshots on failure
    retries: { runMode: 2, openMode: 0 }, // Number of retries on failure
  },

  // Configuration for component testing (if needed)
  component: {
    // Specify the folder where your component test files are located
    specPattern: 'cypress/component/**/*.{js,jsx,ts,tsx}',
    devServer: {
      framework: 'react', // Change according to your framework
      bundler: 'webpack', // Change according to your bundler
    },
  },

  // Other global configurations (if needed)
  reporter: 'spec', // Change the reporter if needed
  pageLoadTimeout: 60000, // Timeout for page loads
});
